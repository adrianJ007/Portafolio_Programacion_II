# Acceso por IP Wi‑Fi

`run.py` escucha en `0.0.0.0:5000` y muestra automáticamente la IP local. Usa la dirección `http://IP_LOCAL:5000` desde equipos conectados a la misma red. En Windows, permite Python únicamente en redes privadas. El servidor Flask de desarrollo no debe publicarse en Internet.
